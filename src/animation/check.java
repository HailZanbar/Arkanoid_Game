package animation;

public class check {
    /*
    public static void main(String[] args) {
        AnimationRunner runner = new AnimationRunner("check", 800, 600);
        Animation a1 = new WinScreen(7);
        Animation a2 = new PauseScreen(); // also an Animation
        Animation a1k = new KeyPressStoppableAnimation(runner.getKeyboard(), "m", a1);
        Animation a2k = new KeyPressStoppableAnimation(runner.getKeyboard(), "m", a2);
        runner.run(a1k);
        runner.run(a2k);
        runner.getGui().close();
    }

     */
}
